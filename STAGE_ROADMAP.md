# Pionex OASIS — 自我进化的活动生成引擎：阶段路线图

**文档版本**：v1.0
**创建日期**：2026-03-30
**愿景转向**：从「SaaS 运营活动模拟平台」→「自我进化的活动生成引擎」

---

## 目录

1. [愿景重定义](#1-愿景重定义)
2. [四阶段详细规划](#2-四阶段详细规划)
   - [Stage 1：评价器（当前 → 3 个月）](#stage-1评价器evaluator)
   - [Stage 2：优化器（3-6 个月）](#stage-2优化器optimizer)
   - [Stage 3：生成器（6-12 个月）](#stage-3生成器generator)
   - [Stage 4：自治引擎（12-18 个月）](#stage-4自治引擎autonomous-engine)
3. [立即执行清单（本周）](#3-立即执行清单本周)
4. [数据飞轮设计](#4-数据飞轮设计)
5. [风险评估与应对](#5-风险评估与应对)
6. [关键里程碑总览](#6-关键里程碑总览)

---

## 1. 愿景重定义

### 1.1 从 SaaS 到引擎：为什么转向

旧路径（SaaS 产品）的核心问题：
- **市场太窄**：加密交易平台运营活动模拟是极垂直的场景，付费用户天花板低
- **价值难量化**：客户买的是"模拟"，但真正需要的是"结果"——高转化率的活动方案
- **护城河浅**：Prompt + API 调用的 SaaS 模式，竞品复制成本极低

新路径（自我进化引擎）的核心逻辑：
- **核心资产是数据和模型**，不是界面和功能
- **价值交付从"工具"变成"方案"**：输入目标，输出可直接执行的活动方案
- **随数据积累护城河越来越深**：每次活动上线都是训练数据，竞品无法复制你的数据闭环

### 1.2 新愿景一句话

> 打造一个能通过喂入真实运营数据不断自我迭代的模型，最终实现：给定业务目标 → 自动生成高转化率和参与率的运营活动方案 → 自动监控效果 → 自动优化迭代。

### 1.3 四阶段演进逻辑

```
Stage 1: 评价器          Stage 2: 优化器          Stage 3: 生成器          Stage 4: 自治引擎
给定方案→模拟反应        给定方案→诊断+建议        给定目标→生成方案        自动监控→自动优化→自动上线

[人类设计方案]          [人类设计方案]            [人类定义目标]           [系统自主运行]
     ↓                      ↓                      ↓                      ↓
[引擎评估打分]          [引擎指出问题]            [引擎生成候选方案]       [引擎监控→诊断→生成→上线]
     ↓                      ↓                      ↓                      ↓
[人类决策是否上线]       [引擎给出优化建议]         [引擎自评估→排序→推荐]   [人类审批关键决策]
                            ↓                      ↓
                        [人类修改后重新评估]        [人类选择→上线→数据回流]
```

### 1.4 与现有 POC 的关系

当前 POC 已验证的能力是 Stage 1 的基础：
- ✅ 多 Agent 模拟反应能力（回测评分 100/100）
- ✅ 参与率预测（误差 ≤5%）
- ✅ 情绪分布预测（平均误差 4%）
- ✅ A/B 方案对比推演
- ✅ 6 类用户画像库

这些是 Stage 1 评价器的核心组件，不需要重建，需要的是**用真实数据校准和强化**。

---

## 2. 四阶段详细规划

---

### Stage 1：评价器（Evaluator）

**时间范围**：当前 → Month 3（2026-04 至 2026-06）
**一句话定义**：给定任意活动方案，输出可靠的参与率/情绪/风险预测评分

#### 核心能力

| 能力 | 描述 | 当前状态 |
|------|------|---------|
| 方案评分 | 输入活动 JSON → 输出参与率、情绪分布、风险标签 | ✅ 已有（需校准） |
| A/B 对比 | 并行评估两套方案，输出量化对比 | ✅ 已有 |
| 画像细分分析 | 按用户画像分解评分，定位关键驱动群体 | ✅ 已有 |
| **真实数据校准** | 用历史活动的真实评论/数据反向校准模型 | ❌ 待建 |
| **评论语气学习** | 从真实评论中学习用户表达风格 | ❌ 待建 |
| **置信度输出** | 每个预测附带置信区间，而非单点数值 | ❌ 待建 |
| **批量回测** | 自动回测多个历史活动，输出校准报告 | ❌ 待建 |

#### 数据需求

| 数据类型 | 数量要求 | 质量要求 | 获取方式 |
|---------|---------|---------|---------|
| 历史活动方案 | ≥10 个活动 | 含完整方案描述、规则、时间、预算 | Pionex 运营团队导出 |
| 活动实际结果 | 对应上述 10 个活动 | 参与率、转化率、留存率、ROI | Pionex 数据仓库 |
| 真实用户评论 | 50-500 条/活动（总计 500-5000 条） | 含时间戳、用户 ID（脱敏）、情绪标注 | 社群抓取 + 客服记录 |
| 用户行为数据 | 覆盖上述活动期间 | 注册→参与→完成→流失漏斗 | Pionex 数据仓库 |
| 竞品活动数据 | 20-50 个 | 方案描述 + 公开社区反馈 | 公开渠道收集 |

#### 技术实现方案

```
┌─────────────────────────────────────────────────┐
│                  Stage 1 架构                     │
├─────────────────────────────────────────────────┤
│                                                   │
│  [活动方案 JSON]                                   │
│       ↓                                           │
│  ┌──────────────────┐    ┌───────────────────┐    │
│  │ Prompt 模板引擎   │←──│ 真实评论语料库     │    │
│  │ (动态 few-shot)   │    │ (按画像/情绪分类)  │    │
│  └────────┬─────────┘    └───────────────────┘    │
│           ↓                                       │
│  ┌──────────────────┐                             │
│  │ 多 Agent 模拟层   │  200-500 Agents             │
│  │ (GPT-4o-mini)     │  6 类画像 × 加权分布         │
│  └────────┬─────────┘                             │
│           ↓                                       │
│  ┌──────────────────┐    ┌───────────────────┐    │
│  │ 评分聚合引擎      │───→│ 校准层             │    │
│  │ (参与率/情绪/风险) │    │ (历史偏差修正系数)  │    │
│  └────────┬─────────┘    └───────────────────┘    │
│           ↓                                       │
│  ┌──────────────────┐                             │
│  │ 输出层            │                             │
│  │ 评分 + 置信区间   │                             │
│  │ + 风险标签        │                             │
│  │ + 画像细分        │                             │
│  └──────────────────┘                             │
│                                                   │
└─────────────────────────────────────────────────┘
```

**关键技术决策**：

1. **Prompt 模板从硬编码改为动态 few-shot**
   - 当前：System Prompt 中硬编码情绪分布指引（如"正面 60-70%"）
   - 改为：从真实评论语料库中动态检索相似活动的评论作为 few-shot 示例
   - 原因：避免"引导式预测"（告诉模型答案再让它预测），让模型从数据中学习

2. **引入校准层（Calibration Layer）**
   - 统计每个画像在不同活动类型下的预测偏差
   - 用线性回归或简单 MLP 学习偏差修正系数
   - 例如：模型对"福利敏感型用户"参与率系统性高估 5%，则校准层自动修正

3. **置信度量化**
   - 对同一方案运行 3-5 次模拟，取均值和标准差
   - 输出格式：`参与率 22% ± 3%（95% CI: 16%-28%）`

#### 验证指标

| 指标 | 目标值 | 验证方法 |
|------|--------|---------|
| 参与率预测 MAE | ≤5% | 对 ≥5 个历史活动回测取平均 |
| 情绪分布 MAE | ≤8% | 对 ≥5 个历史活动回测取平均 |
| 主导情绪方向一致率 | ≥80% | 模拟最高情绪类别 vs 真实最高 |
| 校准覆盖率 | 95% CI 包含真实值 ≥80% | 置信区间验证 |
| 回测通过率 | ≥70%（评分 ≥70/100） | 批量回测 |

#### 预计工时

| 任务 | 工时 | 依赖 |
|------|------|------|
| 真实评论数据收集 + 清洗 | 3 天 | 运营团队配合 |
| 数据标注流程建设 | 2 天 | 评论数据就绪 |
| 评论语料库构建 + 分类 | 3 天 | 标注完成 |
| 动态 few-shot Prompt 引擎 | 5 天 | 语料库就绪 |
| 校准层开发 | 5 天 | ≥3 个历史活动回测 |
| 置信度输出模块 | 2 天 | 校准层就绪 |
| 批量回测框架 | 3 天 | 历史活动数据就绪 |
| 画像库扩展（8→12 类） | 3 天 | 真实用户行为数据 |
| 评价器 API 封装 | 3 天 | 核心模块完成 |
| 端到端测试 + 校准调优 | 5 天 | 全部模块集成 |
| **Stage 1 总计** | **~34 天** | |

---

### Stage 2：优化器（Optimizer）

**时间范围**：Month 3 → Month 6（2026-07 至 2026-09）
**一句话定义**：给定活动方案，不仅评分，还能指出具体问题并给出改进建议

#### 核心能力

| 能力 | 描述 |
|------|------|
| 问题诊断 | 识别方案中的具体弱点（如：门槛过高、奖励不明确、目标人群错位） |
| 改进建议生成 | 针对每个问题输出具体、可执行的修改建议 |
| 建议效果预测 | 对每条建议预测实施后的指标变化（如：降低门槛预计参与率 +5%） |
| 迭代优化循环 | 人类修改方案 → 重新评估 → 再给建议，支持多轮迭代直到方案收敛 |
| 对标分析 | 从历史成功活动库中找到相似案例，对比分析差距 |
| 文案优化 | 优化活动标题、描述、CTA 文案的表达效果 |

#### 数据需求

| 数据类型 | 数量要求 | 质量要求 | 获取方式 |
|---------|---------|---------|---------|
| 活动方案-结果配对 | ≥30 对 | 方案全文 + KPI 结果 + 事后复盘 | Stage 1 积累 + 历史补录 |
| 活动修改历史 | ≥10 个活动的版本变更记录 | 修改前后对比 + 效果对比 | 运营团队文档 |
| 运营知识库 | 50+ 条最佳实践 | 结构化的规则/经验 | 运营团队访谈 + 行业报告 |
| 竞品活动分析 | 100+ 案例 | 方案 + 社区反馈 + 效果（公开数据） | 公开渠道持续收集 |
| 用户流失原因数据 | 1000+ 条 | 退出调查 / 客服记录 / 行为日志 | Pionex 数据仓库 |

#### 技术实现方案

```
┌───────────────────────────────────────────────────────┐
│                   Stage 2 架构                          │
├───────────────────────────────────────────────────────┤
│                                                         │
│  [活动方案]                                              │
│       ↓                                                 │
│  ┌─────────────┐                                        │
│  │ Stage 1      │ → 评分 + 画像细分                       │
│  │ 评价器       │                                        │
│  └──────┬──────┘                                        │
│         ↓                                               │
│  ┌─────────────────────────────────────────┐            │
│  │          诊断引擎（Diagnostic Engine）    │            │
│  ├─────────────────────────────────────────┤            │
│  │ 1. 规则检查器                            │            │
│  │    - 门槛合理性（vs 历史均值）             │            │
│  │    - 奖励吸引力（vs 竞品基准）             │            │
│  │    - 文案清晰度（模糊词检测）              │            │
│  │                                         │            │
│  │ 2. 对标分析器                            │            │
│  │    - 语义检索相似历史活动                  │            │
│  │    - 对比成功/失败案例差异                 │            │
│  │                                         │            │
│  │ 3. 画像弱点定位器                         │            │
│  │    - 哪些画像评分低 → 为什么              │            │
│  │    - 参与漏斗哪一步流失最大               │            │
│  └──────┬──────────────────────────────────┘            │
│         ↓                                               │
│  ┌─────────────────────────────────────────┐            │
│  │          建议生成器（Suggestion Engine）   │            │
│  ├─────────────────────────────────────────┤            │
│  │ - RAG：从运营知识库检索相关最佳实践        │            │
│  │ - LLM：结合诊断结果 + 知识库生成建议       │            │
│  │ - 预测：每条建议附带预计指标变化           │            │
│  │ - 排序：按预计收益/实施难度排序            │            │
│  └──────┬──────────────────────────────────┘            │
│         ↓                                               │
│  ┌─────────────┐                                        │
│  │ 输出          │                                       │
│  │ - 问题列表     │                                       │
│  │ - 建议列表     │                                       │
│  │ - 预计改善幅度  │                                       │
│  │ - 对标案例     │                                       │
│  └─────────────┘                                        │
│         ↓                                               │
│  [人类修改方案] → 重新进入评价器 → 循环                     │
│                                                         │
└───────────────────────────────────────────────────────┘
```

**关键技术决策**：

1. **运营知识库用 RAG 而非微调**
   - 运营知识更新快，RAG 可以随时增删改，微调周期长
   - 向量数据库选型：Qdrant（开源，适合中小规模）或 Pinecone（托管服务）

2. **诊断引擎 = 规则 + 模型混合**
   - 硬规则：门槛金额超过用户平均交易额 2x → 标记"门槛过高"
   - 软规则：LLM 分析文案表达是否清晰、是否有歧义
   - 统计规则：某画像评分显著低于历史均值 → 自动定位原因

3. **建议效果预测用因果推断而非简单统计**
   - 不能说"降低门槛的活动平均参与率更高"→ 可能是混杂因素
   - 使用 DoWhy / CausalML 库做因果效应估计（受数据量限制，初期用简化版本）

#### 验证指标

| 指标 | 目标值 | 验证方法 |
|------|--------|---------|
| 问题识别准确率 | ≥70% | 运营专家盲评诊断报告 |
| 建议采纳率 | ≥50% | 跟踪运营团队是否实施建议 |
| 建议有效率 | 采纳后指标确实改善 ≥60% | 对比修改前后的评价器评分 + 真实结果 |
| 迭代收敛速度 | ≤3 轮迭代方案评分稳定 | 记录每轮评分变化 |
| 运营团队 NPS | ≥40 | 季度用户满意度调查 |

#### 预计工时

| 任务 | 工时 | 依赖 |
|------|------|------|
| 运营知识库构建 + 结构化 | 10 天 | 运营团队访谈 |
| RAG 向量检索系统搭建 | 5 天 | 知识库就绪 |
| 规则检查器开发 | 8 天 | 历史活动数据分析 |
| 对标分析器开发 | 5 天 | 活动语义索引 |
| 画像弱点定位器 | 5 天 | Stage 1 评价器完善 |
| 建议生成器（RAG + LLM） | 8 天 | 知识库 + 检索系统 |
| 建议效果预测模块 | 8 天 | ≥30 对活动数据 |
| 迭代优化循环 UI/API | 5 天 | 核心模块完成 |
| 端到端测试 + 调优 | 8 天 | 全部模块集成 |
| **Stage 2 总计** | **~62 天** | |

---

### Stage 3：生成器（Generator）

**时间范围**：Month 6 → Month 12（2026-10 至 2027-03）
**一句话定义**：给定业务目标和约束条件，自动生成完整的活动方案

#### 核心能力

| 能力 | 描述 |
|------|------|
| 目标解析 | 将业务目标（如"提升 RWA 交易量 30%"）分解为可执行的活动目标 |
| 方案生成 | 自动生成 3-5 个候选活动方案（含机制、文案、预算、时间线） |
| 自评估排序 | 用 Stage 1 评价器对候选方案打分，输出排序 |
| 约束满足 | 在预算、合规、时间等约束条件下生成可行方案 |
| 创意组合 | 从历史成功元素中提取"原子组件"，创造性地重新组合 |
| 多市场适配 | 同一目标生成不同市场（台湾、东南亚）的本地化方案 |

#### 数据需求

| 数据类型 | 数量要求 | 质量要求 | 获取方式 |
|---------|---------|---------|---------|
| 活动方案-结果全量库 | ≥100 对 | 完整方案 + 完整结果 + 标签 | Stage 1-2 积累 + 行业数据 |
| 活动原子组件库 | 200+ 组件 | 分类：机制/奖励/门槛/文案/渠道 | 从历史方案中拆解提取 |
| 业务目标-活动类型映射 | ≥50 条 | 目标→适用活动类型→成功概率 | 运营经验 + 数据分析 |
| 多市场用户画像 | 3+ 市场 × 8+ 画像 | 各市场验证过的画像库 | 各市场运营数据 |
| 合规约束规则库 | 完整 | 各市场的合规要求 | 法务团队 |

#### 技术实现方案

```
┌──────────────────────────────────────────────────────────┐
│                    Stage 3 架构                            │
├──────────────────────────────────────────────────────────┤
│                                                            │
│  [业务目标 + 约束条件]                                       │
│       ↓                                                    │
│  ┌──────────────────────┐                                  │
│  │ 目标解析器             │                                  │
│  │ - 目标分解             │                                  │
│  │ - 约束解析             │                                  │
│  │ - 适用活动类型推荐     │                                  │
│  └──────────┬───────────┘                                  │
│             ↓                                              │
│  ┌──────────────────────────────────────────┐              │
│  │         方案生成引擎（Generation Engine）   │              │
│  ├──────────────────────────────────────────┤              │
│  │                                          │              │
│  │  ┌────────────┐   ┌────────────────┐     │              │
│  │  │ 模板路径    │   │ 创意组合路径    │     │              │
│  │  │ 检索相似    │   │ 拆解原子组件    │     │              │
│  │  │ 成功案例    │   │ 重新组合拼装    │     │              │
│  │  │ 参数化变形  │   │ LLM 创意填充   │     │              │
│  │  └─────┬──────┘   └───────┬────────┘     │              │
│  │        └────────┬─────────┘              │              │
│  │                 ↓                        │              │
│  │  ┌──────────────────────────────┐        │              │
│  │  │ 约束检查 + 合规过滤           │        │              │
│  │  └──────────────────────────────┘        │              │
│  │                 ↓                        │              │
│  │  ┌──────────────────────────────┐        │              │
│  │  │ 输出 3-5 个候选方案           │        │              │
│  │  └──────────────────────────────┘        │              │
│  └──────────┬───────────────────────────────┘              │
│             ↓                                              │
│  ┌──────────────────────┐                                  │
│  │ Stage 1 评价器        │ → 每个候选方案评分                  │
│  └──────────┬───────────┘                                  │
│             ↓                                              │
│  ┌──────────────────────┐                                  │
│  │ Stage 2 优化器        │ → 对 Top 方案进行优化建议            │
│  └──────────┬───────────┘                                  │
│             ↓                                              │
│  ┌──────────────────────┐                                  │
│  │ 排序 + 推荐输出       │                                   │
│  │ - 方案全文            │                                   │
│  │ - 评分 + 置信区间     │                                   │
│  │ - 优化建议            │                                   │
│  │ - 对标案例            │                                   │
│  │ - 预算估算            │                                   │
│  └──────────────────────┘                                  │
│             ↓                                              │
│  [人类选择方案 → 可能再次优化 → 上线 → 数据回流]                │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

**关键技术决策**：

1. **双路径生成（模板 + 创意）**
   - 模板路径：保底，从成功案例变形而来，风险低
   - 创意路径：探索，从原子组件重组，可能产生创新方案
   - 两条路径各生成 2-3 个方案，评价器打分后合并排序

2. **原子组件库设计**
   - 机制组件：返现、抽奖、排行榜、邀请裂变、限时折扣...
   - 奖励组件：现金、积分、实物、VIP 特权、手续费减免...
   - 门槛组件：无门槛、交易额、持仓量、邀请人数...
   - 文案模板：紧迫感型、利益驱动型、社交证明型、FOMO 型...
   - 组件间有兼容性矩阵（如：高门槛 + 低奖励 = 不兼容）

3. **微调 vs Prompt Engineering 决策点**
   - 如果活动方案-结果配对数据 ≥500 对：考虑微调（LoRA）专用生成模型
   - 如果 <500 对：继续使用 Prompt + RAG + 结构化约束

#### 验证指标

| 指标 | 目标值 | 验证方法 |
|------|--------|---------|
| 生成方案可用率 | ≥60%（运营团队认为可执行） | 专家评审 |
| 生成方案评价器评分 | 均值 ≥70/100 | Stage 1 评价器自动评分 |
| 最佳方案 vs 人类方案 | 评价器评分差距 ≤10% | A/B 对比：生成 vs 人工设计 |
| 生成多样性 | 5 个方案间相似度 ≤0.7 | 方案文本语义相似度 |
| 生成速度 | ≤5 分钟/批次 | 端到端计时 |

#### 预计工时

| 任务 | 工时 | 依赖 |
|------|------|------|
| 原子组件库设计 + 构建 | 12 天 | 历史活动全量分析 |
| 组件兼容性矩阵 | 5 天 | 组件库就绪 |
| 目标解析器开发 | 8 天 | 业务目标映射表 |
| 模板路径生成器 | 10 天 | 组件库 + 评价器 |
| 创意路径生成器 | 12 天 | 组件库 + LLM |
| 约束检查 + 合规过滤 | 5 天 | 合规规则库 |
| 多市场适配层 | 10 天 | 多市场画像库 |
| 生成 → 评价 → 优化 Pipeline | 8 天 | Stage 1+2 API 稳定 |
| 端到端测试 + 调优 | 10 天 | 全部模块集成 |
| **Stage 3 总计** | **~80 天** | |

---

### Stage 4：自治引擎（Autonomous Engine）

**时间范围**：Month 12 → Month 18（2027-04 至 2027-09）
**一句话定义**：系统自动监控活动效果 → 自动诊断问题 → 自动生成优化方案 → 经人类审批后自动上线

#### 核心能力

| 能力 | 描述 |
|------|------|
| 实时效果监控 | 活动上线后自动监控关键指标，检测异常和趋势 |
| 自动诊断触发 | 指标偏离预期时自动启动 Stage 2 诊断流程 |
| 自动优化生成 | 基于诊断结果自动生成优化方案或替代方案 |
| 审批工作流 | 关键决策（上线、下线、修改）必须经人类审批 |
| 活动生命周期管理 | 从规划→上线→监控→优化→复盘的全流程自动化 |
| 持续学习 | 每次活动完成后自动更新模型校准参数和知识库 |

#### 数据需求

| 数据类型 | 数量要求 | 质量要求 | 获取方式 |
|---------|---------|---------|---------|
| 实时活动指标流 | 实时 | 延迟 ≤5 分钟 | Pionex 数据平台 API |
| 活动全生命周期数据 | ≥200 个活动 | 从规划到复盘的完整链路 | Stage 1-3 积累 |
| 异常检测基线 | 每个指标的历史分布 | 按活动类型/市场分类 | 统计分析 |
| 审批决策记录 | 完整 | 审批/驳回原因 | 审批系统 |

#### 技术实现方案

```
┌──────────────────────────────────────────────────────────────┐
│                     Stage 4 架构                               │
├──────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                 活动生命周期管理器                        │    │
│  │                                                        │    │
│  │  [规划] → [生成] → [审批] → [上线] → [监控] → [优化] → [复盘] │
│  │    ↑         ↑                         ↓        ↓       │    │
│  │    │      Stage 3                   异常检测  Stage 2    │    │
│  │    │                                   ↓                │    │
│  │    └──────────────── 持续学习 ←── 数据回流 ←─────────────┘    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                │
│  ┌──────────────────┐    ┌──────────────────┐                  │
│  │ 实时监控仪表板    │    │ 人类审批接口      │                  │
│  │ - 指标看板        │    │ - 上线审批        │                  │
│  │ - 异常告警        │    │ - 修改审批        │                  │
│  │ - 趋势预测        │    │ - 紧急下线        │                  │
│  └──────────────────┘    └──────────────────┘                  │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    持续学习系统                            │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │ 1. 校准参数自动更新（每次活动结束后）                       │  │
│  │ 2. 知识库自动扩充（新最佳实践/失败教训）                    │  │
│  │ 3. 画像库自动校准（新用户行为模式）                         │  │
│  │ 4. 组件库自动扩展（新机制/新奖励类型）                      │  │
│  │ 5. 模型周期性重训/微调（月度或季度）                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
└──────────────────────────────────────────────────────────────┘
```

**关键设计原则**：

1. **人类始终在环路中（Human-in-the-Loop）**
   - 自治 ≠ 全自动。关键决策必须人类审批
   - 分级授权：小幅文案修改可自动执行，活动上线/下线必须审批
   - 紧急熔断：任何指标出现剧烈异常，自动暂停活动并通知人类

2. **渐进式放权**
   - Phase A（Month 12-14）：系统建议，人类执行所有操作
   - Phase B（Month 14-16）：系统自动执行低风险操作，高风险需审批
   - Phase C（Month 16-18）：系统自主管理常规活动，人类仅审批创新型活动

3. **可解释性优先**
   - 每个自动决策必须附带完整的推理链条
   - 运营团队可以随时查看"为什么系统做了这个决策"

#### 验证指标

| 指标 | 目标值 | 验证方法 |
|------|--------|---------|
| 异常检测准确率 | ≥90%（召回率 ≥80%） | 历史异常事件回放 |
| 自动优化建议采纳率 | ≥70% | 审批系统记录 |
| 活动 KPI vs 预测偏差 | ≤10% | 批量统计 |
| 人类干预频率 | 逐月递减 | 审批系统记录 |
| 系统管理活动 ROI | ≥人类管理的 80% | 对照实验 |

#### 预计工时

| 任务 | 工时 | 依赖 |
|------|------|------|
| 实时监控系统 | 15 天 | Pionex 数据 API 对接 |
| 异常检测引擎 | 10 天 | 历史指标基线 |
| 自动诊断触发器 | 5 天 | Stage 2 API 稳定 |
| 审批工作流系统 | 10 天 | 组织授权体系 |
| 活动生命周期管理器 | 15 天 | 全部子系统 |
| 持续学习系统 | 15 天 | 数据回流管道 |
| 渐进式放权控制 | 8 天 | 审批系统 |
| 可解释性模块 | 8 天 | 全链路追踪 |
| 端到端测试 + 灰度上线 | 15 天 | 全部模块 |
| **Stage 4 总计** | **~101 天** | |

---

## 3. 立即执行清单（本周）

### 3.1 接入真实评论数据（50-500 条）

**目标**：获取至少一个已完成活动的真实用户评论，建立数据管道原型

| # | 任务 | 负责人 | 产出 | 预计耗时 |
|---|------|--------|------|---------|
| 1 | 确认数据源：社群评论（Telegram/Discord/PTT）还是客服记录 | 运营 + 工程 | 数据源清单 | 2h |
| 2 | 获取 Apple Pay 活动的真实评论（≥50 条） | 运营团队 | `data/real_comments/applepay.jsonl` | 4h |
| 3 | 定义评论数据格式 | 工程 | JSON Schema | 1h |
| 4 | 数据清洗脚本 | 工程 | `scripts/clean_comments.py` | 3h |
| 5 | 导入到本地语料库 | 工程 | `data/corpus/` 目录结构 | 2h |

**评论数据格式（建议）**：

```json
{
  "id": "comment_001",
  "activity_id": "applepay_launch_2026",
  "text": "首刷免單太讚了！已經衝了",
  "author_type": "anonymous",
  "timestamp": "2026-02-15T14:30:00+08:00",
  "source": "telegram",
  "sentiment_label": null,
  "participation_signal": null
}
```

### 3.2 建立数据标注流程

**目标**：建立可持续的标注流程，使每条评论都有情绪标签和参与信号

| # | 任务 | 负责人 | 产出 | 预计耗时 |
|---|------|--------|------|---------|
| 1 | 定义标注体系（情绪 3 级 + 参与信号 3 级） | 工程 + 运营 | 标注指南文档 | 2h |
| 2 | 选择标注工具（推荐：Label Studio 或简单 Spreadsheet） | 工程 | 工具配置 | 2h |
| 3 | 首批标注（50 条，双人标注 + 一致性检查） | 运营 2 人 | 标注数据 v1 | 4h |
| 4 | 计算标注一致性（Cohen's Kappa ≥ 0.7） | 工程 | 一致性报告 | 1h |
| 5 | 用 LLM 辅助标注剩余数据（人工抽检 20%） | 工程 | 半自动标注管道 | 3h |

**标注体系**：

```
情绪标签（sentiment）:
  - positive: 明确表达正面态度（开心、兴奋、认可）
  - neutral: 无明确情绪倾向或纯事实描述
  - negative: 明确表达负面态度（质疑、失望、愤怒）

参与信号（participation_signal）:
  - will_participate: 明确表示会参与（"已经衝了"、"準備開卡"）
  - considering: 犹豫或有条件参与（"看看再說"、"如果...就參加"）
  - will_not_participate: 明确不会参与（"跟我沒關係"、"不信任"）
```

### 3.3 微调 Prompt 学习语气

**目标**：让 Agent 模拟回复的语气更接近真实台湾用户评论风格

| # | 任务 | 负责人 | 产出 | 预计耗时 |
|---|------|--------|------|---------|
| 1 | 从真实评论中提取高频词汇和句式 | 工程 | 语气特征分析 | 2h |
| 2 | 按画像分类整理典型评论（每类 5-10 条） | 工程 | 分类评论样本 | 2h |
| 3 | 重构 Prompt 模板，加入真实评论作为 few-shot | 工程 | 新 Prompt 版本 | 3h |
| 4 | 对比测试：旧 Prompt vs 新 Prompt（同一活动） | 工程 | 对比报告 | 2h |
| 5 | 人工评审：运营团队判断哪个版本更像真人 | 运营 | 评审结论 | 1h |

**本周交付物汇总**：

- [ ] `data/real_comments/applepay.jsonl` — 真实评论数据（≥50 条）
- [ ] `docs/labeling_guide.md` — 标注指南
- [ ] `data/labeled/applepay_labeled.jsonl` — 标注后的评论数据
- [ ] `scripts/clean_comments.py` — 数据清洗脚本
- [ ] `personas/pionex_personas_v2.json` — 更新后的 Prompt 模板（含真实评论 few-shot）
- [ ] `docs/prompt_comparison_report.md` — Prompt 语气对比报告

---

## 4. 数据飞轮设计

### 4.1 飞轮全景

```
                    ┌──────────────────────────────┐
                    │                              │
                    ▼                              │
            ┌──────────────┐                       │
            │  活动方案生成  │                       │
            │  (Stage 3)    │                       │
            └──────┬───────┘                       │
                   │                               │
                   ▼                               │
            ┌──────────────┐                       │
            │  模拟评估     │                       │
            │  (Stage 1)    │                       │
            └──────┬───────┘                       │
                   │                               │
                   ▼                               │
            ┌──────────────┐                       │
            │  人类审批上线  │                       │
            │              │                       │
            └──────┬───────┘                       │
                   │                               │
                   ▼                               │
            ┌──────────────┐      ┌──────────────┐│
            │  活动运行中   │─────→│  数据收集     ││
            │  (真实用户)   │      │  (自动化)     ││
            └──────────────┘      └──────┬───────┘│
                                         │        │
                                         ▼        │
                                  ┌──────────────┐│
                                  │  数据处理     ││
                                  │  标注 + 清洗  ││
                                  └──────┬───────┘│
                                         │        │
                                         ▼        │
                                  ┌──────────────┐│
                                  │  模型更新     ││
                                  │  校准 + 学习  │┘
                                  └──────────────┘
```

### 4.2 数据收集：如何从上线活动中自动获取数据

#### 收集时机与方式

| 数据类型 | 收集时机 | 收集方式 | 存储位置 |
|---------|---------|---------|---------|
| 活动配置快照 | 上线时 | API Hook → 自动存档 | `活动库/configs/` |
| 实时参与指标 | 活动期间（每小时） | Pionex 数据 API 定时拉取 | 时序数据库 |
| 用户评论 | 活动期间 + 结束后 7 天 | 社群爬虫 + 客服系统导出 | `语料库/raw/` |
| 最终 KPI 结果 | 活动结束后 3 天 | 运营团队录入 / 数据仓库导出 | `活动库/results/` |
| 活动复盘记录 | 活动结束后 1 周 | 运营团队填写结构化复盘表 | `知识库/postmortems/` |

#### 自动化管道设计

```python
# 概念示例：数据收集管道
class DataCollectionPipeline:
    """每个活动上线后自动启动的数据收集流程"""

    def on_activity_launch(self, activity_config):
        """活动上线时触发"""
        # 1. 存档活动配置
        self.archive_config(activity_config)
        # 2. 启动定时指标拉取（每小时）
        self.start_metric_polling(activity_config.id, interval_hours=1)
        # 3. 启动社群评论爬虫
        self.start_comment_crawler(activity_config.keywords)

    def on_activity_end(self, activity_id):
        """活动结束时触发"""
        # 1. 拉取最终 KPI
        self.pull_final_kpi(activity_id)
        # 2. 延续评论收集 7 天（捕捉长尾反馈）
        self.extend_crawler(activity_id, extra_days=7)
        # 3. 生成复盘模板，推送给运营团队
        self.send_postmortem_template(activity_id)

    def on_postmortem_complete(self, activity_id):
        """复盘完成时触发"""
        # 1. 整合所有数据
        self.consolidate_data(activity_id)
        # 2. 自动标注评论
        self.auto_label_comments(activity_id)
        # 3. 触发模型更新流程
        self.trigger_model_update(activity_id)
```

### 4.3 数据回流：如何将上线数据反哺模型训练

#### 回流路径

```
真实数据 → 对比分析 → 偏差识别 → 参数校准 → 模型更新
```

#### 具体回流机制

**1. 校准参数更新（每次活动结束后，自动执行）**

```
步骤：
1. 计算 预测值 vs 真实值 的偏差
2. 按画像分解偏差来源
3. 更新校准层的修正系数
4. 用最近 N 个活动的数据做加权平均（越新权重越高）

示例：
- 模型连续 3 次高估"活跃合约交易者"参与率 → 自动下调该画像修正系数
- 模型在"返现类活动"上预测一致偏低 → 自动上调返现类活动的基线
```

**2. 知识库扩充（每次复盘后，半自动执行）**

```
步骤：
1. 从复盘记录中提取结构化经验
2. LLM 辅助将经验转化为知识库条目
3. 人工审核后入库
4. 更新 RAG 向量索引

示例输入（复盘记录）：
"这次排行榜活动参与率低于预期，主要因为规则说明太复杂，用户看不懂怎么参与"

自动提取的知识库条目：
{
  "type": "lesson_learned",
  "category": "activity_design",
  "rule": "排行榜类活动规则说明应控制在 3 步以内",
  "evidence": "活动 X 参与率低于预期 8%，用户反馈集中在'规则看不懂'",
  "confidence": 0.8
}
```

**3. 画像库更新（季度，人工主导）**

```
步骤：
1. 分析最近一个季度所有活动的画像预测偏差
2. 识别是否出现新的用户行为模式
3. 决定是否新增/合并/拆分画像
4. 用新数据重新校准画像参数
```

**4. 模型重训/微调（半年度或数据量达到阈值时）**

```
触发条件（满足任一）：
- 活动方案-结果配对数据新增 ≥100 条
- 校准层修正系数偏移 >20%
- 连续 5 个活动预测偏差 >10%

重训流程：
1. 整理全量训练数据（方案 + 结果 + 评论）
2. 重新微调 Prompt / LoRA 权重
3. 在留出集上验证
4. A/B 对比新旧模型
5. 灰度切换
```

### 4.4 效果评估：如何衡量模型迭代是否有效

#### 核心评估指标

| 指标 | 计算方法 | 目标趋势 |
|------|---------|---------|
| **预测 MAE 趋势** | 最近 10 个活动的参与率预测平均绝对误差 | 逐季度下降 |
| **校准覆盖率** | 95% 置信区间包含真实值的比例 | 逐季度上升至 ≥85% |
| **建议采纳率** | 运营团队采纳优化建议的比例 | 逐月上升 |
| **建议有效率** | 采纳后指标确实改善的比例 | 逐季度上升 |
| **数据回流完整率** | 每个活动数据完整收集的比例 | ≥90% |

#### 评估仪表板（建议）

```
月度模型健康报告
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
本月活动数：8
数据完整率：87.5%（7/8 完整收集）

预测精度趋势：
  参与率 MAE：4.2% → 3.8% → 3.5%  ↓ 改善
  情绪分布 MAE：6.1% → 5.3% → 5.0%  ↓ 改善

校准覆盖率：82% → 85%  ↑ 改善

偏差最大的活动：活动 X（参与率偏差 +12%）
  原因分析：新活动类型（首次做 NFT 空投），缺少历史对标
  改进措施：已将该活动加入训练集，新增 NFT 类活动标签

建议有效率：63%（上月 58%） ↑ 改善
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 5. 风险评估与应对

### 5.1 数据量不足

**风险等级**：🔴 高（尤其是 Stage 1-2 阶段）

**具体场景**：
- 真实评论数据 <50 条，无法有效学习语气
- 活动方案-结果配对 <10 对，校准层无法训练
- 某些活动类型只有 1-2 个历史案例

**应对策略**：

| 策略 | 适用场景 | 具体做法 |
|------|---------|---------|
| **合成数据增强** | 评论数据不足 | 用 LLM 基于少量真实评论生成风格一致的合成评论，标记为合成数据，权重低于真实数据 |
| **竞品数据补充** | 活动案例不足 | 收集竞品平台（Binance/OKX/Bybit）的公开活动和社区反馈，作为参考数据（非校准用） |
| **跨域迁移** | 特定活动类型缺数据 | 将"手续费返还"类活动的经验迁移到其他"确定性奖励"类活动，共享校准参数 |
| **Bootstrap 采样** | 统计量不稳定 | 对小样本数据进行 Bootstrap 重采样，估计置信区间而非点估计 |
| **渐进式积累** | 长期策略 | 确保每次活动的数据完整回流，3-6 个月后数据量自然增长 |

**最低可行数据量（MVM — Minimum Viable Model）**：

```
Stage 1 启动需要：
  - 真实评论：≥50 条（1 个活动）
  - 历史活动：≥3 个（含完整 KPI）
  - 用户画像验证：≥1 个活动的画像分布对照

Stage 2 启动需要：
  - 活动方案-结果配对：≥30 对
  - 运营知识条目：≥50 条
  - 修改前后对比数据：≥10 个案例

Stage 3 启动需要：
  - 活动方案-结果全量库：≥100 对
  - 原子组件库：≥200 个组件
```

### 5.2 因果推断难题

**风险等级**：🟡 中

**具体场景**：
- "降低门槛导致参与率提升"还是"低门槛活动本身就面向容易参与的人群"？
- 修改文案后效果变好——是文案的作用还是时间窗口（如节日效应）的作用？
- 竞品同期推出类似活动，影响了你的活动效果

**应对策略**：

| 策略 | 复杂度 | 适用阶段 | 说明 |
|------|--------|---------|------|
| **控制变量法** | 低 | Stage 1-2 | A/B 测试时只改变一个变量，其他保持不变 |
| **倾向得分匹配（PSM）** | 中 | Stage 2-3 | 当无法做 A/B 测试时，用统计方法模拟对照组 |
| **双重差分法（DID）** | 中 | Stage 2-3 | 利用时间维度的变化来识别因果效应 |
| **工具变量法（IV）** | 高 | Stage 3-4 | 找到只影响自变量不直接影响结果的工具变量 |
| **因果贝叶斯网络** | 高 | Stage 4 | 建立变量间的因果关系图，进行结构化推断 |

**务实建议**：

> Stage 1-2 阶段不要追求严格的因果推断。承认相关性，标注置信度，让人类做最终判断。
>
> 把精力放在"预测准不准"而非"为什么准"上。当预测稳定准确后（Stage 3），再投资因果推断能力。

### 5.3 生成质量不稳定

**风险等级**：🟡 中（Stage 3 核心风险）

**具体场景**：
- 同一输入，多次生成的方案质量差异大
- 生成的方案看似合理但实际不可执行（如预算超标、合规问题）
- 创意路径生成的方案过于"奇怪"，运营团队不信任

**应对策略**：

| 策略 | 说明 |
|------|------|
| **多次生成 + 自评估** | 每次生成 10 个候选方案，用评价器自动筛选 Top 3-5 |
| **约束前置检查** | 在生成前将硬约束（预算、合规、时间）编码为结构化输入 |
| **可行性评分** | 除了效果评分，增加"可行性评分"（技术可实现、运营可执行、预算可承受） |
| **保守 + 激进分层** | 明确标注：哪些方案是"安全选择"（模板路径），哪些是"探索性尝试"（创意路径） |
| **人类反馈强化** | 记录运营团队对每个方案的评价（采纳/驳回/修改原因），用于改进生成器 |
| **温度/采样控制** | 降低 LLM 温度可提高一致性，升高可提高创意性，根据场景动态调整 |

### 5.4 其他风险

| 风险 | 等级 | 影响 | 应对 |
|------|------|------|------|
| **LLM API 成本失控** | 🟡 中 | Stage 3-4 生成大量候选方案 | 使用缓存、批处理、本地模型降本 |
| **模型幻觉** | 🟡 中 | 生成不存在的竞品案例或编造数据 | 所有引用必须可溯源，增加事实检查层 |
| **过拟合历史** | 🟡 中 | 模型只会生成历史上成功过的方案类型 | 强制创意路径占比 ≥30%，定期引入外部灵感 |
| **运营团队不信任** | 🔴 高 | 系统建议被忽略，数据飞轮无法转动 | 从小胜开始，透明展示推理过程，逐步建立信任 |
| **数据泄露/合规** | 🔴 高 | 用户数据经 LLM API 处理 | 数据脱敏、使用私有部署模型、遵守数据保护法规 |
| **关键人员流失** | 🟡 中 | 核心工程师或运营对接人变动 | 完善文档、模块化架构、知识不绑定个人 |

---

## 6. 关键里程碑总览

### 时间线视图

```
2026 Q2 (Apr-Jun)          2026 Q3 (Jul-Sep)          2026 Q4 (Oct-Dec)          2027 Q1 (Jan-Mar)          2027 Q2-Q3 (Apr-Sep)
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
│ Stage 1: 评价器          │ Stage 2: 优化器          │ Stage 3: 生成器（上半）   │ Stage 3: 生成器（下半）   │ Stage 4: 自治引擎       │
│                          │                          │                          │                          │                         │
│ M1: 真实数据接入         │ M4: 诊断引擎上线         │ M7: 原子组件库完成        │ M9: 多市场适配           │ M11: 实时监控上线        │
│ M2: 校准层完成           │ M5: 知识库 RAG 上线      │ M8: 方案生成器 Alpha     │ M10: 生成器 Beta         │ M12: 自治引擎 Alpha     │
│ M3: 评价器 v2 发布       │ M6: 优化器 Beta          │                          │                          │ M13: 自治引擎 Beta      │
│                          │                          │                          │                          │ M14: 正式上线            │
```

### 里程碑详情

| # | 里程碑 | 目标日期 | 交付物 | 验收标准 |
|---|--------|---------|--------|---------|
| M1 | 真实数据接入 | 2026-04-07 | 评论数据管道 + ≥50 条标注数据 | 数据格式标准化，标注一致性 κ≥0.7 |
| M2 | 校准层完成 | 2026-05-15 | 校准模块 + ≥5 个活动回测报告 | 回测 MAE ≤5%，通过率 ≥80% |
| M3 | 评价器 v2 发布 | 2026-06-30 | 评价器 API + 置信度输出 + 文档 | 外部用户可调用，置信度覆盖率 ≥80% |
| M4 | 诊断引擎上线 | 2026-07-31 | 规则检查 + 对标分析 + 弱点定位 | 问题识别准确率 ≥60%（运营专家评审） |
| M5 | 知识库 RAG 上线 | 2026-08-31 | 向量检索 + 知识库 ≥50 条 | 检索相关性 ≥70%（人工评估） |
| M6 | 优化器 Beta | 2026-09-30 | 完整优化循环 + 建议生成 | 建议采纳率 ≥40%（运营试用反馈） |
| M7 | 原子组件库完成 | 2026-10-31 | ≥200 组件 + 兼容性矩阵 | 覆盖 ≥90% 历史活动类型 |
| M8 | 方案生成器 Alpha | 2026-11-30 | 双路径生成 + 自评估 | 生成方案可用率 ≥40%（内部测试） |
| M9 | 多市场适配 | 2027-01-31 | ≥2 个市场画像库 | 每个市场回测 MAE ≤8% |
| M10 | 生成器 Beta | 2027-03-31 | 完整生成 → 评价 → 优化 Pipeline | 生成方案可用率 ≥60%，运营团队试用 |
| M11 | 实时监控上线 | 2027-05-31 | 监控仪表板 + 异常检测 | 异常检测准确率 ≥80% |
| M12 | 自治引擎 Alpha | 2027-07-31 | 自动诊断 + 建议 + 审批工作流 | 运营团队开始使用审批流程 |
| M13 | 自治引擎 Beta | 2027-08-31 | 渐进式放权 Phase A 完成 | 低风险操作自动化率 ≥50% |
| M14 | 正式上线 | 2027-09-30 | 全功能自治引擎 | 系统管理活动 ROI ≥ 人类管理的 80% |

### 总工时汇总

| 阶段 | 预计工时 | 时间范围 | 核心产出 |
|------|---------|---------|---------|
| Stage 1：评价器 | ~34 天 | Month 1-3 | 可靠的活动方案评分系统 |
| Stage 2：优化器 | ~62 天 | Month 3-6 | 活动方案诊断 + 优化建议系统 |
| Stage 3：生成器 | ~80 天 | Month 6-12 | 目标驱动的活动方案自动生成 |
| Stage 4：自治引擎 | ~101 天 | Month 12-18 | 全流程自动化活动管理 |
| **总计** | **~277 天** | **18 个月** | **自我进化的活动生成引擎** |

---

**文档创建**：2026-03-30 · Deep Analyst Agent
**下次评审**：2026-04-13（Stage 1 第一轮数据接入验收后）
